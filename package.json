{
  "name": "ai-dataflow",
  "version": "1.0.0",
  "private": true,
  "description": "AI DataFlow production MVP",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp *.html public/ && cp -R css js images public/",
    "start": "node server.js",
    "dev": "node server.js",
    "smoke": "node qa-smoke.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
